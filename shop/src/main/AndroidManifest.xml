<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.example.christian.cleantest.shop"
   xmlns:android="http://schemas.android.com/apk/res/android">

   <application>

      <activity android:name=".core.ui.ShopActivity" android:noHistory="true">
         <intent-filter>
            <action android:name="${ACTION_SHOP}" />
            <category android:name="android.intent.category.DEFAULT" />
         </intent-filter>
      </activity>
   </application>
</manifest>