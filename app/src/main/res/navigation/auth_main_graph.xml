<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
   xmlns:app="http://schemas.android.com/apk/res-auto" android:id="@+id/main_graph"
   app:startDestination="@id/authFeatureFragment"
   >

   <fragment android:id="@+id/authFeatureFragment"
      android:name="com.distribution.christian.cleantest.auth.core.ui.AuthFeatureFragment"
      android:label="AuthFeatureFragment"
      >
      <action android:id="@+id/action_authFeatureFragment_to_auth_graph"
         app:destination="@id/auth_graph"
         />
   </fragment>
   <include app:graph="@navigation/auth_graph" />
</navigation>