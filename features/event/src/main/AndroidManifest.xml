<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.distribution.christian.cleantest.event"
   xmlns:android="http://schemas.android.com/apk/res/android">

   <application>

      <!--<activity
         android:name="com.distribution.christian.cleantest.event.presentation.stars.StarsActivity"
         android:theme="@style/AppTheme.Dialog">
         <intent-filter>
            <action android:name="${ACTION_STARS}" />
            <category android:name="android.intent.category.DEFAULT" />
         </intent-filter>
      </activity>-->
   </application>

</manifest>