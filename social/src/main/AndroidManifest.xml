<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.distribution.christian.cleantest.social"
   xmlns:android="http://schemas.android.com/apk/res/android">

   <application>

      <activity android:name="com.distribution.christian.cleantest.social.core.ui.SocialActivity">
         <intent-filter>
            <action android:name="${ACTION_SOCIAL}" />
            <category android:name="android.intent.category.DEFAULT" />
         </intent-filter>
      </activity>
   </application>

</manifest>