package com.distribution.christian.cleantest.core.core.navigation

import com.christian.multinavlib.navigation.coordinator.CoordinatorManager

enum class FeatureStates : CoordinatorManager.State {
   EVENTS,
   SHOP,
   PROFILE,
   AUTH
}